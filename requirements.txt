# Part 2 Notebook C (the centerpiece) needs none of this; it is stdlib only.
# The rest is for Notebooks A, B, D and Part 3.
# Verified working on Python 3.14 with the versions noted.

numpy>=1.26          # tested 2.5.2
pandas>=2.1          # tested 3.0.5
scikit-learn>=1.4    # tested 1.9.0
matplotlib>=3.8      # tested 3.x

# Note: Part 3 computes its fairness rates by hand, so fairlearn is not required.
